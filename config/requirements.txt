pandas>=2.0.0
numpy>=1.24.0
pytest>=7.4.0
Flask>=3.0.0
alpaca-py>=0.33.0
keyring>=24.0.0
qrcode[pil]==8.2
requests>=2.31.0
# Market data import: .parquet files (pyarrow) and .7z archives (py7zr).
# .zip archives use Python's built-in zipfile, no extra dependency needed.
# Both optional in spirit -- .csv/.tsv/.txt import works with neither
# installed; only opening a .parquet or .7z file raises a clear
# "pip install ..." error if the corresponding package is missing.
pyarrow>=14.0.0
py7zr>=0.20.0
# Research library (research/ folder) -- extracts text from PDF papers you
# drop in there so the Ollama AI assist can be grounded in them. Optional
# in spirit: if this isn't installed, PDF papers are silently skipped
# (.txt/.md papers still work with no extra dependency at all) rather than
# the app failing to start.
pypdf>=4.0.0
# Portfolio Optimizer's long-only Markowitz solve (app/quant_lab/portfolio_optimizer.py)
# and any other constrained-optimization use. Optional in spirit: the
# unconstrained (short-selling-allowed) closed-form solver needs no
# external library at all, and the long-only path falls back to a
# clip-and-renormalize approximation (clearly labeled as such) if scipy
# isn't installed, rather than failing.
scipy>=1.11.0
# Live Market's "open chart" button: opens a chromeless native window
# instead of a browser tab when available, since Tkinter itself has no
# way to run the real JavaScript/Canvas content Lightweight Charts needs.
# Optional -- the app falls back to your default browser (fully
# functional either way) if this isn't installed or fails to start.
pywebview>=5.0; platform_system == "Windows"
# Forward Test (MT5 Demo) tab only -- talks to a locally-running MT5
# terminal, which only exists on Windows. Harmless to leave in on
# other platforms: pip simply skips it there, and the Forward Test tab
# detects its absence and explains why instead of erroring.
MetaTrader5>=5.0.45; platform_system == "Windows"
